import React from "react";
import classes from "./stats.module.scss";

const Stats: React.FC = () => {
  return (
    <React.Fragment>
      <div className={classes.mainContainer}>Statspage</div>
    </React.Fragment>
  );
};

export default Stats;
